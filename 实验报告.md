# Rust 课程期末作业报告

## RustFinder 本地文件搜索与内容索引工具

**学院：** 南开大学软件学院  
**课程：** Rust 程序设计  
**项目名称：** RustFinder 本地文件搜索与内容索引工具  
**姓名：** 许宁远  
**学号：** 2414065  
**完成方式：** 单人完成  
**日期：** 2026 年 5 月 29 日

---

## 一、项目简介

### 1.1 项目背景

在日常学习和开发过程中，本地目录中经常会积累大量代码文件、Markdown 文档、配置文件、日志文件和编译产物。如果只依赖资源管理器手动查找文件，效率较低；如果每次都直接遍历目录搜索内容，在文件数量较多时又会产生重复开销。因此，本项目设计并实现了一个基于 Rust 的本地文件搜索与内容索引工具 **RustFinder**。

RustFinder 的核心思想是：先扫描指定目录，提取文件的基础元数据并建立本地索引；之后用户可以基于索引快速执行文件名搜索、内容搜索、条件过滤、统计分析和结果导出。项目定位为一个本地命令行系统工具，符合课程作业鼓励方向中的“系统工具、命令行工具、数据处理工具、文件系统工具”。

### 1.2 项目功能概述

RustFinder 实现了以下功能：

1. 扫描指定目录，递归读取文件信息。
2. 建立本地索引文件，默认保存到 `.rust_finder/index.rfidx`。
3. 按文件名或路径搜索文件。
4. 在文本文件内容中搜索关键词。
5. 支持显示内容搜索的上下文行。
6. 支持按扩展名过滤文件。
7. 支持按文件大小过滤文件。
8. 支持按修改日期过滤文件。
9. 支持只查看文本文件或二进制文件。
10. 支持按路径、文件名、大小、修改时间、扩展名排序。
11. 支持限制输出结果数量。
12. 生成索引统计报告，包括文件数量、总大小、扩展名分布、最大文件、最新文件等。
13. 将结果导出为 CSV 或 JSON。
14. 自动跳过 `.git`、`target`、`node_modules`、`.rust_finder` 等常见无关目录。
15. 提供统一、清晰的终端输出格式，长路径可以自动换行。

### 1.3 项目特点

本项目不是简单的增删改查系统，而是一个围绕文件系统扫描、索引存储、条件查询、内容匹配和统计分析展开的完整命令行工具。实现过程中较多使用 Rust 的核心语言特性，包括结构体、枚举、trait、Result 错误处理、所有权与借用、模块化组织和单元测试。

项目代码主要位于 `src` 目录中，共包含 13 个 Rust 源文件。当前 `src` 下 Rust 源码约 2300 行，满足单人项目建议规模要求。

### 1.4 GitHub 链接

GitHub 仓库链接：

```text
（此处填写最终提交的 GitHub 仓库链接；如果通过压缩包提交，可以写“以源码压缩包形式提交”。）
```

---

## 二、小组成员分工（单人不需要）

本项目为单人完成项目，因此不存在小组分工。项目的选题、需求分析、架构设计、编码实现、测试、文档编写和报告整理均由本人独立完成。

---

## 三、项目结构

### 3.1 目录结构

项目主要结构如下：

```text
RustFinder/
├── Cargo.toml
├── Cargo.lock
├── README.md
├── 功能示例.md
├── 实验报告.md
└── src/
    ├── main.rs
    ├── cli.rs
    ├── config.rs
    ├── error.rs
    ├── model.rs
    ├── scanner.rs
    ├── index.rs
    ├── filter.rs
    ├── search.rs
    ├── stats.rs
    ├── export.rs
    ├── output.rs
    └── util.rs
```

### 3.2 文件说明

| 文件             | 主要作用                                                                      |
| ---------------- | ----------------------------------------------------------------------------- |
| `Cargo.toml`     | Rust 项目配置文件，定义项目名称、版本、edition 和 release 配置                |
| `README.md`      | 项目简介、基础使用方式、模块说明                                              |
| `功能示例.md`    | 各功能的详细命令示例，便于演示和复习                                          |
| `实验报告.md`    | 本实验报告                                                                    |
| `src/main.rs`    | 程序入口，负责解析命令并分发到不同功能模块                                    |
| `src/cli.rs`     | 命令行参数解析，定义 `scan`、`find`、`grep`、`list`、`stats`、`export` 等命令 |
| `src/config.rs`  | 默认配置，包括忽略目录、文本扩展名、扫描限制等                                |
| `src/error.rs`   | 统一错误类型 `AppError` 和结果类型 `AppResult<T>`                             |
| `src/model.rs`   | 核心数据结构，包括文件记录、索引、搜索结果、排序字段等                        |
| `src/scanner.rs` | 目录扫描、文件元数据读取、文本文件识别、行数统计                              |
| `src/index.rs`   | 索引文件保存和加载                                                            |
| `src/filter.rs`  | 扩展名、大小、日期、文件类型等过滤条件                                        |
| `src/search.rs`  | 文件名搜索与内容搜索                                                          |
| `src/stats.rs`   | 统计报告生成                                                                  |
| `src/export.rs`  | CSV 和 JSON 导出                                                              |
| `src/output.rs`  | 终端输出格式化                                                                |
| `src/util.rs`    | 通用工具函数，如大小解析、日期解析、转义、校验值计算等                        |

### 3.3 模块划分思想

项目采用按职责拆分的模块化设计。命令解析、业务模型、扫描、索引、搜索、过滤、统计、导出和输出各自独立，降低了模块之间的耦合度。这样做有几个优点：

1. 每个模块职责清晰，便于阅读和维护。
2. 错误处理方式统一，避免在各处重复处理异常。
3. 搜索、过滤、统计等逻辑可以分别测试。
4. 后续扩展功能时，可以在对应模块内修改，不需要大范围改动。

---

## 四、设计与实现

### 4.1 整体设计思路

RustFinder 的执行流程可以概括为以下几个阶段：

1. 用户在终端输入命令。
2. `cli.rs` 解析命令行参数，生成内部命令枚举 `Command`。
3. `main.rs` 根据命令类型分发到具体模块。
4. 如果是 `scan`，调用 `scanner.rs` 扫描目录，生成 `FileIndex`，再由 `index.rs` 保存到磁盘。
5. 如果是 `find`、`grep`、`list`、`stats`、`export`，先由 `index.rs` 读取索引，再执行搜索、过滤、统计或导出。
6. 最后由 `output.rs` 统一格式化输出结果。

该设计将“扫描建索引”和“基于索引查询”分开，避免每次搜索都重新遍历整个目录，提高重复查询效率。

### 4.2 命令设计

项目支持 6 个主要命令：

| 命令     | 功能               |
| -------- | ------------------ |
| `scan`   | 扫描目录并建立索引 |
| `find`   | 按文件名或路径搜索 |
| `grep`   | 搜索文本文件内容   |
| `list`   | 列出索引中的文件   |
| `stats`  | 输出统计报告       |
| `export` | 导出 CSV 或 JSON   |

示例命令：

```powershell
cargo run -- scan .
cargo run -- find README
cargo run -- grep Rust --text --context 1
cargo run -- list --ext rs --sort size --desc
cargo run -- stats
cargo run -- export csv result.csv --ext rs
```

命令解析没有依赖第三方库，而是使用标准库手动解析参数。这样虽然代码量更大，但能更直接体现 Rust 中字符串处理、枚举建模、错误返回和模式匹配的使用。

### 4.3 索引设计

索引由 `FileIndex` 表示，内部包含：

1. 扫描根目录 `root`。
2. 索引创建时间 `created_at`。
3. 文件记录列表 `records`。
4. 跳过项列表 `skipped`。

每个文件记录由 `FileRecord` 表示，包含：

1. 文件路径。
2. 文件名。
3. 扩展名。
4. 文件大小。
5. 修改时间。
6. 是否为文本文件。
7. 文本文件行数。
8. 文件内容校验值。

索引默认写入：

```text
.rust_finder/index.rfidx
```

每次执行：

```powershell
cargo run -- scan .
```

都会重新扫描并覆盖默认索引文件，因此索引不会像日志一样无限追加增长。

### 4.4 扫描设计

扫描模块 `scanner.rs` 使用迭代方式遍历目录。程序维护一个目录队列，从根目录开始，将子目录加入队列，逐个读取文件信息。这样避免了过深递归可能带来的调用栈问题。

扫描过程中会进行以下处理：

1. 判断路径是否存在。
2. 判断路径是否为目录。
3. 读取目录项。
4. 跳过默认忽略目录。
5. 判断文件类型。
6. 读取文件元数据。
7. 读取文件前若干字节作为样本。
8. 判断是否为文本文件。
9. 对文本文件统计行数。
10. 生成文件记录。

默认跳过的名称包括：

```text
.git
.hg
.svn
target
node_modules
.rust_finder
__pycache__
.idea
.vscode
```

这些目录通常是版本控制、构建缓存或编辑器配置目录，不适合作为普通搜索对象。

### 4.5 文本文件识别

项目通过两种方式判断文件是否为文本文件：

1. 根据扩展名判断，例如 `.rs`、`.md`、`.txt`、`.json`、`.toml` 等。
2. 如果扩展名未知，则读取文件样本，检查是否存在空字节，并尝试按 UTF-8 解析。

如果文件为空，也会被视为文本文件，因为空文件没有二进制内容特征。

### 4.6 文件名搜索设计

`find` 命令通过 `SearchEngine::find_by_name` 实现。它会遍历索引中的文件记录，判断文件名或完整路径是否包含用户输入的关键词。

默认情况下，搜索是大小写不敏感的。例如：

```powershell
cargo run -- find readme
```

可以匹配：

```text
README.md
```

如果用户添加 `--case-sensitive`，则使用大小写敏感搜索。

### 4.7 内容搜索设计

`grep` 命令通过 `SearchEngine::search_content` 实现。它只对文本文件进行逐行读取，判断每一行是否包含关键词。

例如：

```powershell
cargo run -- grep Rust --text --context 1
```

表示在文本文件中搜索 `Rust`，并显示命中行前后各一行。

为了实现上下文功能，程序维护一个前文队列 `VecDeque`，保存最近的若干行；当命中关键词时，将这些前文行加入结果。对于后文行，则在命中后继续收集指定数量的行。

### 4.8 过滤设计

过滤逻辑位于 `filter.rs`，由 `FilterSet` 统一表示。支持的过滤条件包括：

1. 扩展名过滤：`--ext rs`
2. 最小大小：`--min-size 10kb`
3. 最大大小：`--max-size 1mb`
4. 修改日期下限：`--modified-after 2026-05-01`
5. 修改日期上限：`--modified-before 2026-06-01`
6. 仅文本文件：`--text`
7. 仅二进制文件：`--binary`

过滤条件可以组合使用，例如：

```powershell
cargo run -- list --ext rs --min-size 1kb --sort size --desc
```

表示列出 `.rs` 文件中大于 1KB 的文件，并按大小从大到小排序。

### 4.9 排序设计

排序字段使用枚举 `SortField` 表示，支持：

```text
path
name
size
modified
ext
```

排序方向使用 `SortDirection` 表示，支持：

```text
asc
desc
```

使用枚举而不是字符串直接传递，可以让非法排序字段在解析阶段就被发现，从而减少运行时错误。

### 4.10 统计设计

统计模块 `stats.rs` 根据索引生成 `StatsReport`。报告内容包括：

1. 根目录。
2. 索引创建时间。
3. 文件总数。
4. 文本文件数量。
5. 二进制文件数量。
6. 文件总大小。
7. 平均文件大小。
8. 已知文本行数。
9. 扩展名分布。
10. 最大文件。
11. 最新修改文件。
12. 最旧修改文件。
13. 跳过项数量。

统计功能能够直观展示索引内容，也适合在演示视频中体现项目完整性。

### 4.11 导出设计

导出模块 `export.rs` 支持两种格式：

1. CSV：适合用 Excel 打开查看。
2. JSON：适合进一步程序化处理。

导出内容不是文件正文，而是文件记录信息，包括路径、文件名、扩展名、大小、修改时间、是否文本文件、行数和校验值。

示例：

```powershell
cargo run -- export csv result.csv
cargo run -- export json result.json --text
```

导出命令也支持过滤和排序，因此可以只导出部分文件：

```powershell
cargo run -- export csv rust_files.csv --ext rs
```

### 4.12 输出格式设计

早期版本使用表格式输出，但在 Windows 中文路径较长时，表格容易被撑开，显示不美观。因此后续将输出改为“字段名 + 值”的纵向格式。例如：

```text
README.md
path       D:\许宁远\学习\Rust\大作业\新建文件夹\README.md
size       1.50 KB
type       text
ext        md
lines      60
modified   2026-05-29 17:43:00
```

这种格式的优点是：

1. 每个字段单独一行，阅读更清楚。
2. 长路径不会挤压其他列。
3. `find`、`list`、`grep`、`stats` 等输出风格统一。
4. 适合终端截图和演示视频。

---

## 五、各模块详细说明

### 5.1 `main.rs`：程序入口与命令分发

`main.rs` 是程序入口。它主要完成以下工作：

1. 调用 `Cli::parse` 解析命令行参数。
2. 创建默认配置 `AppConfig`。
3. 创建终端输出对象 `Printer`。
4. 使用 `match` 根据命令类型分发到不同逻辑。
5. 统一处理错误，如果发生错误则输出错误信息并以非零状态码退出。

该模块体现了 Rust 中 `Result` 错误处理和 `match` 模式匹配的典型用法。

核心流程类似：

```rust
fn main() {
    if let Err(err) = run() {
        eprintln!("error: {err}");
        std::process::exit(1);
    }
}
```

这样可以避免在各处直接 `panic`，提高程序健壮性。

### 5.2 `cli.rs`：命令行参数解析

`cli.rs` 定义了命令行接口。主要数据结构包括：

1. `Cli`
2. `Command`
3. `ScanOptions`
4. `FindOptions`
5. `GrepOptions`
6. `ListOptions`
7. `StatsOptions`
8. `ExportOptions`
9. `QueryFilterOptions`

其中 `Command` 是一个枚举，用于表示用户选择的命令：

```rust
pub enum Command {
    Help,
    Scan(ScanOptions),
    Find(FindOptions),
    Grep(GrepOptions),
    List(ListOptions),
    Stats(StatsOptions),
    Export(ExportOptions),
}
```

这种设计的优点是每个命令都携带自己需要的参数，后续在 `main.rs` 中进行模式匹配时，类型清晰，不容易混淆。

命令解析过程中如果遇到未知命令、缺少参数、非法排序字段等情况，会返回 `AppError::InvalidArgument`，而不是直接崩溃。

### 5.3 `config.rs`：默认配置

`config.rs` 保存项目默认配置，包括：

1. 是否启用彩色输出。
2. 是否跟随符号链接。
3. 读取文本样本的最大大小。
4. 统计行数的最大文件大小。
5. 默认忽略目录。
6. 默认认为是文本文件的扩展名。

例如：

```text
.git
target
node_modules
.rust_finder
.vscode
```

这些默认规则可以避免扫描大量无意义的构建缓存文件，使搜索结果更干净。

### 5.4 `error.rs`：统一错误处理

`error.rs` 定义：

```rust
pub type AppResult<T> = Result<T, AppError>;
```

所有可能失败的操作都尽量返回 `AppResult<T>`。`AppError` 枚举包括：

1. IO 错误。
2. 数字解析错误。
3. 参数错误。
4. 索引格式错误。
5. 索引文件不存在。
6. 日期格式错误。
7. 不支持的导出格式。

这种方式比大量使用 `unwrap()` 更安全，也符合课程对 Rust 错误处理的要求。

### 5.5 `model.rs`：核心数据结构

`model.rs` 是项目的数据模型模块。重要结构包括：

#### 5.5.1 `FileRecord`

表示一个文件的索引记录，包含：

```text
path
name
extension
size
modified
is_text
line_count
checksum
```

#### 5.5.2 `FileIndex`

表示整个索引，包含根目录、创建时间、文件记录列表和跳过项列表。

#### 5.5.3 `ContentMatch`

表示内容搜索的命中结果，包括文件路径、行号、命中行、前文行和后文行。

#### 5.5.4 `SortField` 与 `SortDirection`

用于表示排序字段和排序方向。

该模块大量使用结构体和枚举，是体现 Rust 建模能力的重要部分。

### 5.6 `scanner.rs`：目录扫描与文本识别

`scanner.rs` 负责实际扫描文件系统。主要功能：

1. 校验扫描根目录是否存在。
2. 使用队列递归遍历目录。
3. 跳过默认忽略项。
4. 读取文件元数据。
5. 读取文件样本。
6. 判断文本文件。
7. 统计文本文件行数。
8. 生成 `FileRecord`。

扫描过程中遇到无法读取的文件或目录，不会直接使程序崩溃，而是记录到 `skipped` 中，并继续处理其他文件。这种设计提高了程序在真实文件系统环境中的容错性。

### 5.7 `index.rs`：索引保存与加载

`index.rs` 负责将 `FileIndex` 保存到磁盘，以及从磁盘读取索引。

本项目没有使用数据库，而是设计了一个简单文本索引格式。索引文件开头包含魔数：

```text
RUST_FINDER_INDEX_V1
```

这样加载索引时可以先检查格式版本，避免误读其他文件。

索引保存时会对特殊字符进行转义，避免路径或字段中包含换行、制表符等字符导致解析错误。

### 5.8 `filter.rs`：过滤条件

`filter.rs` 将命令行参数转换为内部过滤器 `FilterSet`。它负责：

1. 校验过滤条件是否冲突。
2. 解析日期。
3. 解析大小。
4. 判断某个 `FileRecord` 是否满足条件。
5. 对结果进行排序。

例如 `--text` 和 `--binary` 不能同时使用，如果同时出现，会返回参数错误。

### 5.9 `search.rs`：搜索功能

`search.rs` 是搜索核心模块。它定义了：

1. `SearchEngine`
2. `ContentSearchOptions`
3. `Matcher<T>` trait
4. `NameMatcher`
5. `LineMatcher`

其中 `Matcher<T>` 是一个抽象匹配接口：

```rust
pub trait Matcher<T: ?Sized> {
    fn matches(&self, value: &T) -> bool;
}
```

`NameMatcher` 实现文件名匹配，`LineMatcher` 实现文本行匹配。这体现了 Rust trait 在抽象行为方面的作用。

### 5.10 `stats.rs`：统计报告

`stats.rs` 根据索引生成统计报告。主要结构：

```rust
pub struct StatsReport {
    pub root: String,
    pub created_at: String,
    pub total_files: usize,
    pub text_files: usize,
    pub binary_files: usize,
    pub total_size: u64,
    ...
}
```

统计结果通过 `output.rs` 统一展示。该模块体现了对索引数据的聚合分析能力。

### 5.11 `export.rs`：导出功能

`export.rs` 支持导出 CSV 和 JSON。导出过程中需要处理：

1. JSON 字符串转义。
2. CSV 字段转义。
3. 空扩展名处理。
4. 可读大小显示。
5. 文本文件行数为空时的处理。

导出文件可以用 Excel、文本编辑器或其他程序继续分析。

### 5.12 `output.rs`：终端输出格式化

`output.rs` 负责所有终端展示，包括：

1. 扫描摘要。
2. 文件搜索结果。
3. 内容搜索结果。
4. 统计报告。
5. 帮助信息。

该模块后期进行了重点优化，将原来的宽表格改为纵向字段输出，解决了中文路径较长时表格错位的问题。

### 5.13 `util.rs`：通用工具函数

`util.rs` 包含多个辅助函数，例如：

1. 扩展名标准化。
2. 大小字符串解析，例如 `10kb`、`2mb`。
3. 文件大小可读化，例如 `3.22 KB`。
4. 日期解析。
5. 时间戳显示。
6. 文件样本校验值计算。
7. 字段转义和反转义。

这些工具函数被多个模块复用，避免了重复代码。

---

## 六、运行截图

本报告 Markdown 文件中不直接插入截图，最终提交 Word 或 PDF 报告时建议补充以下截图。下面列出每张截图应包含的内容。

### 6.1 编译截图

命令：

```powershell
cargo build
```

截图内容：展示项目可以成功编译。

如果使用 `cargo run` 时已经自动编译，也可以截取第一次运行命令时的编译输出。

### 6.2 扫描目录截图

命令：

```powershell
cargo run --quiet -- scan .
```

截图内容应包括：

```text
Scan completed
root
files
text files
binary files
total size
skipped
```

该截图用于证明程序能够扫描目录并建立索引。

### 6.3 统计报告截图

命令：

```powershell
cargo run --quiet -- stats
```

截图内容应包括：

```text
Index statistics
Top extensions
Largest file
Newest file
Oldest file
```

该截图用于展示统计功能。

### 6.4 文件名搜索截图

命令：

```powershell
cargo run --quiet -- find README
```

截图内容应包括：

```text
file(s) matched
path
size
type
ext
lines
modified
```

该截图用于展示按文件名搜索功能。

### 6.5 内容搜索截图

命令：

```powershell
cargo run --quiet -- grep Rust --text --context 1
```

截图内容应包括命中文件路径、行号、命中行和上下文行。

### 6.6 过滤与排序截图

命令：

```powershell
cargo run --quiet -- list --ext rs --sort size --desc --limit 5
```

截图内容应展示只列出 `.rs` 文件，并按照文件大小排序。

### 6.7 导出结果截图

命令：

```powershell
cargo run --quiet -- export csv rust_files.csv --ext rs
```

截图内容应展示导出成功，例如：

```text
exported 13 records to rust_files.csv
```

也可以再补一张打开 `rust_files.csv` 的截图，展示 CSV 文件内容。

---

## 七、遇到的问题与解决方法

### 7.1 长路径导致终端表格错位

最初文件搜索结果使用表格形式输出，字段包括路径、大小、类型、扩展名、修改时间和行数。但是 Windows 下项目路径较长，并且路径中包含中文字符，导致路径列过宽，终端显示时出现错位。

解决方法：

1. 放弃单行宽表格输出。
2. 改为纵向字段显示。
3. 每个字段单独占一行。
4. 长路径自动换行。
5. `size`、`type`、`ext`、`lines`、`modified` 分别显示。

修改后的输出更适合终端阅读，也更适合截图展示。

### 7.2 构建目录被扫描导致统计结果混乱

在开发过程中，Cargo 会生成 `target` 目录，里面包含 `.o`、`.rmeta`、`.d` 等大量编译中间产物。如果这些文件被扫描进索引，会导致扩展名统计结果中出现很多与项目源码无关的类型。

解决方法：

1. 默认忽略 `target`。
2. 默认忽略 `.rust_finder`。
3. 默认忽略 `.git`、`node_modules` 等无关目录。
4. 在提交作业时不打包构建目录。

这样可以使扫描结果更接近用户真正关心的文件。

### 7.3 索引文件自身被扫描的问题

索引文件默认保存在 `.rust_finder/index.rfidx` 中。如果扫描时不跳过 `.rust_finder`，程序可能把自己的索引文件也加入索引，造成无意义结果。

解决方法：在默认忽略目录中加入 `.rust_finder`。

### 7.4 文本文件与二进制文件的判断

不同类型文件没有统一可靠的文本判断方式。单纯依赖扩展名不够准确，但直接读取整个文件又可能影响性能。

解决方法：

1. 对常见文本扩展名直接识别。
2. 对未知扩展名读取前若干字节作为样本。
3. 如果样本包含空字节，则认为可能是二进制文件。
4. 如果样本能按 UTF-8 解析，则认为是文本文件。

这种方法在简单工具中比较实用。

### 7.5 错误处理不能依赖 `unwrap`

文件系统操作很容易失败，例如路径不存在、权限不足、文件被占用、索引不存在等。如果大量使用 `unwrap()`，程序会直接崩溃，不符合课程对错误处理的要求。

解决方法：

1. 定义统一错误类型 `AppError`。
2. 使用 `AppResult<T>` 作为统一返回类型。
3. 在上层集中输出错误。
4. 对可跳过的扫描错误记录到 `skipped`，不影响其他文件扫描。

### 7.6 PowerShell 和 Cargo 输出混在一起

使用：

```powershell
cargo run -- find README
```

时，终端会先显示 Cargo 的编译和运行提示，例如 `Finished`、`Running`。这不是程序自身输出，容易影响演示观感。

解决方法：演示时使用：

```powershell
cargo run --quiet -- find README
```

这样可以隐藏 Cargo 自身提示，只显示程序输出。

---

## 八、其他需要说明的内容（如果有）

### 8.1 关于 AI 使用情况

本项目开发过程中使用了 AI 辅助进行选题分析、代码结构规划、部分代码生成、文档整理和报告润色。AI 的作用主要是辅助实现和提高效率，项目功能设计、最终代码调整、运行测试、输出格式修改和报告内容确认均由本人参与完成。

在使用 AI 辅助时，重点进行了以下人工检查：

1. 检查功能是否符合课程要求。
2. 检查代码是否能体现 Rust 特性。
3. 检查模块划分是否清晰。
4. 根据实际运行效果修改输出格式。
5. 根据作业模板整理实验报告。

### 8.2 关于第三方依赖

本项目主要使用 Rust 标准库实现，没有引入额外第三方依赖。这样做的优点是：

1. 降低环境配置难度。
2. 减少网络依赖。
3. 便于在课程环境中直接编译。
4. 更能体现对 Rust 标准库和基础语法的掌握。

### 8.3 关于提交注意事项

提交源码时不应包含构建目录，例如：

```text
target/
build/
build2/
.rust_finder_build_test/
```

这些目录属于编译缓存或临时构建产物，体积较大，不属于项目源码。提交时应保留：

```text
Cargo.toml
Cargo.lock
README.md
功能示例.md
实验报告.md
src/
```

### 8.4 关于项目局限性

目前 RustFinder 仍有一些可以继续改进的地方：

1. 没有实现真正的增量索引，每次 `scan` 都会重新扫描并覆盖索引。
2. 内容搜索目前是逐行读取文件，没有建立倒排索引。
3. 文本文件判断是启发式的，不能保证对所有文件类型完全准确。
4. 没有实现图形界面或 TUI 界面。
5. 没有使用多线程扫描，面对特别大的目录时仍有优化空间。

这些局限性不影响本项目作为课程期末作业的完整性，但可以作为后续扩展方向。

---

## 九、总结

通过本次 RustFinder 本地文件搜索与内容索引工具的开发，我对 Rust 语言和工程实践有了更系统的理解。

首先，在语言特性方面，项目使用了结构体、枚举、trait、模式匹配、Result 错误处理、所有权和借用等核心内容。特别是在索引、搜索和过滤功能中，Rust 的类型系统帮助程序保持了较好的结构清晰度。

其次，在工程组织方面，项目按功能拆分为多个模块，包括命令解析、配置、错误处理、数据模型、扫描、索引、过滤、搜索、统计、导出和输出。这样的结构比把所有代码写在一个文件中更清晰，也更接近真实项目开发方式。

再次，在错误处理方面，项目没有依赖大量 `unwrap()`，而是通过 `AppError` 和 `AppResult<T>` 统一管理错误。文件系统操作中的权限错误、路径错误、索引不存在、参数非法等情况都可以给出相对友好的提示。

此外，项目还实现了较完整的用户功能：扫描、搜索、过滤、排序、统计和导出，能够形成一个从建立索引到查看结果再到导出文件的完整使用流程。该流程适合在 5 分钟以内的视频中展示，也能体现项目的实用性。

本项目相比简单 CRUD 管理系统，更能体现 Rust 在系统工具和命令行工具开发中的优势。通过完成该项目，我进一步掌握了 Rust 文件系统编程、模块化设计、错误传播、数据建模和终端工具开发方法，达到了本课程期末大作业对功能完整性、Rust 特性使用、工程结构和文档能力的要求。
