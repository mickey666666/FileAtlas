# RustFinder 功能示例

下面列出 RustFinder 的所有主要功能和示例命令。示例默认在项目目录下运行。

## 1. 使用前先扫描

大部分命令都依赖索引文件，所以第一次使用前要先扫描目录。

```powershell
cargo run -- scan .
```

扫描后会生成默认索引：

```text
.rust_finder/index.rfidx
```

## 2. scan：扫描目录

扫描当前目录：

```powershell
cargo run -- scan .
```

扫描 `src` 目录：

```powershell
cargo run -- scan ./src
```

扫描上一级目录：

```powershell
cargo run -- scan ..
```

使用自定义索引文件：

```powershell
cargo run -- scan . --index my_index.rfidx
```

跟随符号链接扫描：

```powershell
cargo run -- scan . --follow-links
```

演示时隐藏 Cargo 编译提示：

```powershell
cargo run --quiet -- scan .
```

## 3. find：按文件名或路径搜索

搜索 README 文件：

```powershell
cargo run -- find README
```

搜索名字里包含 `main` 的文件：

```powershell
cargo run -- find main
```

搜索名字里包含 `report` 的文件：

```powershell
cargo run -- find report
```

只搜索 Rust 源码文件：

```powershell
cargo run -- find main --ext rs
```

只搜索 Markdown 文件：

```powershell
cargo run -- find README --ext md
```

限制最多显示 5 条结果：

```powershell
cargo run -- find src --limit 5
```

按修改时间从新到旧排序：

```powershell
cargo run -- find src --sort modified --desc
```

区分大小写搜索：

```powershell
cargo run -- find README --case-sensitive
```

使用自定义索引搜索：

```powershell
cargo run -- find main --index my_index.rfidx
```

## 4. grep：搜索文件内容

在文本文件中搜索 `Rust`：

```powershell
cargo run -- grep Rust --text
```

搜索 `Result`：

```powershell
cargo run -- grep Result --text
```

只在 `.rs` 文件中搜索：

```powershell
cargo run -- grep Result --ext rs
```

显示命中行前后各 1 行：

```powershell
cargo run -- grep Rust --text --context 1
```

使用短参数显示上下文：

```powershell
cargo run -- grep Rust --text -C 2
```

限制最多显示 10 条命中：

```powershell
cargo run -- grep error --text --limit 10
```

区分大小写搜索：

```powershell
cargo run -- grep FileRecord --ext rs --case-sensitive
```

搜索 Markdown 文件里的关键词：

```powershell
cargo run -- grep index --ext md --context 1
```

使用自定义索引搜索内容：

```powershell
cargo run -- grep Result --index my_index.rfidx
```

## 5. list：列出文件

列出所有已索引文件：

```powershell
cargo run -- list
```

只列出 Rust 文件：

```powershell
cargo run -- list --ext rs
```

只列出 Markdown 文件：

```powershell
cargo run -- list --ext md
```

只列出文本文件：

```powershell
cargo run -- list --text
```

只列出二进制文件：

```powershell
cargo run -- list --binary
```

按文件大小从大到小列出：

```powershell
cargo run -- list --sort size --desc
```

按文件名排序：

```powershell
cargo run -- list --sort name --asc
```

只显示前 20 个文件：

```powershell
cargo run -- list --limit 20
```

列出最近修改的 10 个文件：

```powershell
cargo run -- list --sort modified --desc --limit 10
```

## 6. 过滤功能

过滤参数可以用于 `find`、`grep`、`list`、`export`。

按扩展名过滤：

```powershell
cargo run -- list --ext rs
```

同时过滤多个扩展名：

```powershell
cargo run -- list --ext rs --ext md
```

筛选大于 10KB 的文件：

```powershell
cargo run -- list --min-size 10kb
```

筛选小于 1MB 的文件：

```powershell
cargo run -- list --max-size 1mb
```

筛选 1KB 到 100KB 之间的文件：

```powershell
cargo run -- list --min-size 1kb --max-size 100kb
```

筛选某日期之后修改的文件：

```powershell
cargo run -- list --modified-after 2026-05-01
```

筛选某日期之前修改的文件：

```powershell
cargo run -- list --modified-before 2026-06-01
```

筛选日期范围内修改的文件：

```powershell
cargo run -- list --modified-after 2026-05-01 --modified-before 2026-06-01
```

组合过滤：Rust 文件 + 大小限制 + 排序：

```powershell
cargo run -- list --ext rs --min-size 1kb --sort size --desc
```

## 7. 排序功能

支持的排序字段：

```text
path
name
size
modified
ext
```

按路径排序：

```powershell
cargo run -- list --sort path
```

按文件名排序：

```powershell
cargo run -- list --sort name
```

按大小从小到大排序：

```powershell
cargo run -- list --sort size --asc
```

按大小从大到小排序：

```powershell
cargo run -- list --sort size --desc
```

按修改时间从新到旧排序：

```powershell
cargo run -- list --sort modified --desc
```

按扩展名排序：

```powershell
cargo run -- list --sort ext
```

## 8. stats：统计报告

查看默认索引统计：

```powershell
cargo run -- stats
```

查看自定义索引统计：

```powershell
cargo run -- stats --index my_index.rfidx
```

演示时隐藏 Cargo 编译提示：

```powershell
cargo run --quiet -- stats
```

统计报告包括：

```text
索引根目录
索引创建时间
文件总数
文本文件数量
二进制文件数量
总大小
平均文件大小
已知文本行数
跳过的文件或目录数量
扩展名分布
最大文件
最新修改文件
最旧修改文件
```

## 9. export：导出结果

导出所有文件为 CSV：

```powershell
cargo run -- export csv result.csv
```

导出所有文件为 JSON：

```powershell
cargo run -- export json result.json
```

只导出 Rust 文件：

```powershell
cargo run -- export csv rust_files.csv --ext rs
```

只导出 Markdown 文件：

```powershell
cargo run -- export json markdown_files.json --ext md
```

只导出文本文件：

```powershell
cargo run -- export csv text_files.csv --text
```

导出大文件列表：

```powershell
cargo run -- export json large_files.json --min-size 1mb
```

导出最近修改的前 10 个文件：

```powershell
cargo run -- export csv recent.csv --sort modified --desc --limit 10
```

使用自定义索引导出：

```powershell
cargo run -- export csv src_files.csv --index my_index.rfidx
```

## 10. 自定义索引完整示例

给 `src` 单独建索引：

```powershell
cargo run -- scan ./src --index src_index.rfidx
```

从这个索引中搜索文件名：

```powershell
cargo run -- find main --index src_index.rfidx
```

从这个索引中搜索内容：

```powershell
cargo run -- grep Result --index src_index.rfidx
```

查看这个索引的统计：

```powershell
cargo run -- stats --index src_index.rfidx
```

导出这个索引中的文件：

```powershell
cargo run -- export csv src_files.csv --index src_index.rfidx
```

## 11. 推荐演示流程

录制 5 分钟以内演示视频时，可以按下面顺序展示：

```powershell
cargo run --quiet -- scan .
cargo run --quiet -- stats
cargo run --quiet -- find README
cargo run --quiet -- list --ext rs --sort size --desc --limit 5
cargo run --quiet -- grep Rust --text --context 1
cargo run --quiet -- export csv rust_files.csv --ext rs
```

这个流程可以覆盖：

- 扫描建索引
- 统计报告
- 文件名搜索
- 条件过滤
- 排序
- 内容搜索
- 导出结果

## 12. 输出位置

终端直接显示结果的命令：

```text
scan
find
grep
list
stats
help
```

会写入文件的命令：

```text
scan    生成 .rust_finder/index.rfidx
export  生成用户指定的 csv/json 文件
```

例如：

```powershell
cargo run -- export csv rust_files.csv --ext rs
```

会生成：

```text
rust_files.csv
```
