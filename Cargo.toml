[package]
name = "rust_finder"
version = "0.1.0"
edition = "2024"
description = "A local file search and content indexing tool written in Rust."
license = "MIT"

[profile.release]
strip = true
lto = true
codegen-units = 1

